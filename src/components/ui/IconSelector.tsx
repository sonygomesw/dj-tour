'use client' import { getLevelInfo, getLevelIcon } from '@/components/icons/level-icons' import { GlassContainer } from './GlassContainer' // Composant pour afficher toutes les icônes de niveau export function AllIconsPreview() { const levels = [1, 2, 3, 4, 5] return ( <GlassContainer className="p-8"> <div className="text-center mb-8"> <h2 className="text-2xl font-bold text-gray-900 mb-2"> Icônes de Niveau </h2> <p className="text-gray-600"> Chaque niveau a son icône unique </p> </div> <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-8"> {levels.map(level => { const levelInfo = getLevelInfo(level) const LevelIcon = getLevelIcon(level) return ( <div key={level} className="text-center"> <div className="mb-4"> <h3 className="text-lg font-semibold text-gray-900 mb-1"> {levelInfo.emoji} {levelInfo.name} </h3> <p className="text-sm text-gray-600">{levelInfo.description}</p> </div> <div className="flex justify-center"> <div className={` w-16 h-16 rounded-2xl flex items-center justify-center bg-gradient-to-br ${levelInfo.colors.gradient} shadow-lg transition-transform hover:scale-110 border-2 border-white/20 `}> <LevelIcon className="w-8 h-8 text-gray-900"/> </div> </div> <p className="text-xs text-gray-500 mt-3"> Niveau {level} </p> </div> ) })} </div> </GlassContainer> ) } 