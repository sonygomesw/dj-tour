'use client' import { FC } from 'react' export const MissionProgress: FC = () => { // Mock data - to be replaced with real data later const progress = 65 const totalPoints = 1000 const earnedPoints = 650 return ( <div className="space-y-4"> <div className="flex justify-between items-center"> <span className="text-gray-700">Progression totale</span> <span className="text-gray-900 font-medium">{progress}%</span> </div> <div className="h-4 bg-white/10 rounded-full overflow-hidden"> <div className="h-full bg-gradient-to-r from-purple-500 to-blue-500 rounded-full transition-all duration-300"style={{ width: `${progress}%` }} /> </div> <div className="flex justify-between items-center text-sm"> <span className="text-gray-600">{earnedPoints} points earned</span> <span className="text-gray-600">{totalPoints} points total</span> </div> </div> ) } 