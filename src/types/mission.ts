export enum MissionCategory { PREPARATION = 'Preparation', CONTENT = 'Content', VISIBILITY = 'Visibility', BOOKING = 'Booking', NETWORKING = 'Networking' } export enum MissionStatus { AVAILABLE = 'available', IN_PROGRESS = 'in_progress', COMPLETED = 'completed', LOCKED = 'locked' } export interface MissionStep { id: string title: string description?: string completed: boolean completedAt?: string } export interface Mission { id: string title: string description: string category: MissionCategory status: MissionStatus difficulty: 'beginner' | 'intermediate' | 'advanced' points: number duration: string icon: string steps: string[] rewards?: { xp: number badges?: string[] unlockMissions?: string[] } tips?: string[] resources?: { title: string url: string type: 'video' | 'article' | 'tool' | 'template' }[] } export interface UserMission { id: string user_id: string mission_id: string status: 'not_started' | 'in_progress' | 'completed' progress: number // 0-100 completed: boolean completed_at?: string started_at?: string user_notes?: string unlock_missions?: string[] created_at: string updated_at: string steps?: MissionStep[] } export interface UserStats { id: string user_id: string total_xp: number current_level: number missions_completed: number missions_in_progress: number created_at: string updated_at: string } export interface UserProgress { totalMissions: number completedMissions: number totalPoints: number currentMission: number // Current mission number (1-100) nextMission: number // Next mission to unlock progress: number // Overall progress (0-100) } 